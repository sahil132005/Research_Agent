# ─────────────────────────────────────────────
#  Research Brief Generator — Environment Config
# ─────────────────────────────────────────────
#  Fill in ONE of the two keys below, leave the other blank.
#  The agent will auto-detect which one is set.

# Option A — Grok API key (from https://console.x.ai)
GROK_API_KEY=""

# Option B — OpenAI API key (from https://platform.openai.com)
OPENAI_API_KEY=""
# ── Optional overrides ────────────────────────
# Change the model if needed. Defaults are set automatically
# based on which key is active (grok-3 or gpt-4o).
# MODEL=grok-3
# MODEL=gpt-4o
